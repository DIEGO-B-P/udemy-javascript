const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;
console.log(`Operador soma ${soma}`);
console.log(`Operador subtração ${subtracao}`);
console.log(`Operador multiplicação ${multiplicacao}`);
console.log(`Operador divisão ${divisao}`);
console.log(`Operador modulo/resto ${modulo}`);
